{
  "name": "SocialMediaSettings",
  "type": "object",
  "properties": {
    "clickup_list_id": {
      "type": "string",
      "description": "ClickUp list ID where 'GP - Social Posts' tasks are created for this client"
    },
    "clickup_workspace_id": {
      "type": "string",
      "description": "Optional ClickUp workspace (team) ID, used to fetch task attachments and (if needed) the Brand Reference Guide doc"
    },
    "clickup_brand_doc_url": {
      "type": "string",
      "description": "Full ClickUp Doc URL for the Brand Reference Guide; the doc and page IDs are parsed from this URL automatically"
    },
    "brand_guide_text": {
      "type": "string",
      "description": "Fallback/manual Brand Reference Guide text used if the ClickUp doc URL isn't set or can't be fetched"
    },
    "site_url": {
      "type": "string",
      "description": "Base site URL used to build GBP Learn More button URLs with UTM tracking"
    },
    "postiz_facebook_id": {
      "type": "string",
      "description": "Postiz integration ID for the client's Facebook account"
    },
    "postiz_instagram_id": {
      "type": "string",
      "description": "Postiz integration ID for the client's Instagram account"
    },
    "postiz_x_id": {
      "type": "string",
      "description": "Postiz integration ID for the client's X (Twitter) account"
    },
    "postiz_gmb_id": {
      "type": "string",
      "description": "Postiz integration ID for the client's Google Business profile"
    },
    "postiz_linkedin_id": {
      "type": "string",
      "description": "Postiz integration ID for the client's LinkedIn account or company page"
    },
    "short_links": {
      "type": "array",
      "description": "Short links per page and platform, used when linking generated posts to landing pages",
      "items": {
        "type": "object",
        "properties": {
          "page": {
            "type": "string",
            "description": "App page path the short link points to (e.g. '/services/specials')"
          },
          "platform": {
            "type": "string",
            "enum": [
              "facebook",
              "instagram",
              "twitter",
              "linkedin",
              "google_business",
              "all"
            ],
            "description": "Platform this short link is intended for, or 'all' for any platform"
          },
          "url": {
            "type": "string",
            "description": "The short link URL"
          }
        },
        "required": [
          "page",
          "platform",
          "url"
        ]
      },
      "default": []
    }
  },
  "required": [
    "clickup_list_id"
  ]
}